version: '3' 
services:
  web: 
    build: 
      context: ${DIR_DFILES}
      dockerfile: ./dockerfile
    env_file: 
      - ./pCine/build/.env
    ports: 
      - ${PUERTO}:8080
    # volumes: 
    #   - ../app_next:/usr/share/nginx/html
